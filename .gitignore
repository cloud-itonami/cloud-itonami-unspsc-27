.cpcache
/.eastwood
/target
/classes
/.nrepl-port
/hprof
# Local absolute-path dep override for dev outside the monorepo (worktree in
# /tmp etc.). Never commit -- contains machine-specific absolute :local/root.
deps.local.edn
