{:paths ["src"]
 ;; langgraph-clj brings langchain-clj transitively (zero-/single-dep,
 ;; all .cljc -> portable to JVM / SCI / ClojureScript / GraalVM). It lives
 ;; in the com-junkawasaki org of this workspace. This repo is standalone
 ;; (forkable outside the workspace); the :local/root paths only resolve
 ;; inside the monorepo checkout -- a standalone fork should override them
 ;; with git coordinates.
 ;;
 ;; Unlike cloud-itonami-isic-6910, there is NO corporate-intel cross-ref
 ;; (formation.corporate-intel is not ported -- no analog in the tool-fleet
 ;; domain), so the cloud-itonami-isic-8291 dep is intentionally absent.
 :deps {io.github.com-junkawasaki/langgraph-clj
        {:local/root "../../com-junkawasaki/langgraph-clj"}}
 :aliases
 {;; offline workspace dev -- pin transitive langchain to local checkout.
  ;; (langgraph-clj declares langchain under the io.github.kotoba-lang/langchain
  ;; coordinate, so that is the coordinate overridden here.)
  :dev {:override-deps
        {io.github.kotoba-lang/langchain
         {:local/root "../../com-junkawasaki/langchain-clj"}}}
  ;; clojure -M:dev:run  -> drive the demo through one OperationActor
  :run {:main-opts ["-m" "formation.sim"]}
  :test {:extra-paths ["test"]
         :extra-deps {io.github.cognitect-labs/test-runner
                      {:git/tag "v0.5.1" :git/sha "dfb30dd"}}
         :main-opts ["-m" "cognitect.test-runner"]
         :exec-fn cognitect.test-runner.api/test}
  ;; static analysis -- clj-kondo from deps (no binary), errors fail CI.
  :lint {:replace-deps {clj-kondo/clj-kondo {:mvn/version "2024.11.14"}}
         :main-opts    ["-m" "clj-kondo.main"
                        "--lint" "src" "test"
                        "--fail-level" "error"]}}}
